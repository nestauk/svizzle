/*
addFileToData
addToLoadingURIs
computeProgress
computeTargets
deleteFileFetcher
deleteUneededFetchers
pullFromLoadingURIs
spawnNewFetchers
*/

const addToLoadingURIs = () => {};

export const fetchManagerOptions = {
	actions: {
		/**
		 * Initialize the context
		 */
		addToLoadingURIs: assign(addToLoadingURIs),
	},
	guards: {
	}
};
