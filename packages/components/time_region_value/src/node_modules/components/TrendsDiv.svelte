<script>
	import TrendsG from 'components/TrendsG.svelte';

	export let colorScale;
	export let data;
	export let formatFn;
	export let getIndicatorValue;
	export let schema;
	export let theme;
	export let types;
	export let useRankScale = false;

	let height;
	let width;
</script>

<div
	bind:clientHeight={height}
	bind:clientWidth={width}
	class='TrendsDiv'
>
	{#if data && width && height}
		<svg
			height={height}
			width={width}
		>
			<TrendsG
				{colorScale}
				{data}
				{formatFn}
				{getIndicatorValue}
				{schema}
				{theme}
				{types}
				{useRankScale}
				height={height}
				width={width}
			/>
		</svg>
	{/if}
</div>

<style>
	.TrendsDiv {
		height: 100%;
		width: 100%;
	}
</style>
